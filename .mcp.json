{
  "mcpServers": {
    "radar": {
      "command": "uv",
      "args": [
        "run",
        "radar",
        "serve",
        "--repo",
        "data/seed_repo",
        "--index-path",
        ".radar-index"
      ]
    }
  }
}
